export interface Dev {
  name: string;
  role: string;
  description: string;
  photoUrl: string;
  social: {
    linkedin?: string;
    instagram?: string;
    github?: string;
    website?: string;
  };
}